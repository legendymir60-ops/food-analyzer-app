name: Build Kivy APK

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Build APK
        run: |
          pip3 install buildozer cython
          buildozer android debug
        timeout-minutes: 60
